name: Daily News Agent (Simple Test)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello World! The workflow is working."
